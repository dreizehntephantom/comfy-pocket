# ===== НАСТРОЙКИ IPADAPTER РЕНДЕРА =====
# Как config.txt, плюс ref_dir (папка референсов) и weight (сила референса).
# Для КАЖДОЙ картинки из ref_dir делается count вариаций.
# Промпт/негатив берутся из тех же prompt.txt / negative.txt.

model = anillustriousXL_v14
lora = sasaminanime
lora_weight = 0.8

steps = 25
cfg = 4
width = 1024
height = 1536

# Папка, ОТКУДА брать картинки-референсы (png/jpg/jpeg/webp/bmp). Путь без кавычек.
ref_dir = refs

# СИЛА референса: 0.2-0.3 = лёгкий намёк (как ты привык), 0.5-0.7 = заметно, 1.0 = сильно.
weight = 0.3

# Сколько вариаций на КАЖДУЮ картинку из ref_dir.
count = 30
