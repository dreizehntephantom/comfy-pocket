# ===== НАСТРОЙКИ ИНПЕЙНТА ИЗ PHOTOSHOP =====
# Это то, что НЕ меняется от кнопки к кнопке. Остальное (контекст, разрешение,
# denoise, промпт) панель присылает сама.
# Меняй значение после знака = . Строки с # — комментарии.

model = anillustriousXL_v14
lora = sasaminanime
lora_weight = 0.8

# VAE: пусто = взять из самой модели (работает всегда, на любой машине).
# Впиши имя файла, только если нужен отдельный — например sdxl-vae-fp16-fix
vae =

steps = 25
cfg = 5

# denoise: 0.4-0.6 — подправить/детализировать то, что есть.
# 0.75-1.0 — переодеть во что-то другое. Панель может это перебить.
denoise = 0.6

# растушёвка края маски в пикселях (тот самый mask_blend)
mask_blend = 32

# Сила, с которой держится форма по объёму, когда в панели включён depth.
# 0.6 — как в РЕНДЕР_ПОЗА. Если карту снимать только с выделения, объём давит
# сильнее и цифру обычно приходится опускать до 0.4-0.5.
depth_strength = 0.6

# Промпт и негатив берутся из prompt.txt / negative.txt,
# если в панели поле промпта пустое.

# Сила, с которой держится палитра, когда в панели включён цвет.
# Шкала тут ДРУГАЯ, не как у глубины: адаптер лёгкий, на 0.6 его почти не
# слышно. Померено 2026-08-19: 1.5 — палитра приходит, рисунок чистый;
# 2.0 — цвет давит, начинается мыло; 3.0 — разваливается.
color_strength = 1.5

# По какому куску имени искать адаптер цвета в ComfyUI\models\controlnet\ .
color_model = t2i
