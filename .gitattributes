# Храним и достаём файлы байт в байт, ничего не переделывая.
#
# Зачем. Без этой строки гит при выкачке на другой машине дописывает
# в конец каждой строки лишний байт. Для .bat это опасно: cmd читает
# такой файл кусками определённого размера, и сдвиг байтов уводит
# границу куска в середину русской буквы — команда разваливается.
#
# То есть у нас работает, а у того, кто скачал, — нет. И проверить
# заранее нельзя.
#
# Когда все .bat переедут в Python, эта защита станет не нужна,
# но и мешать не будет.

* -text
