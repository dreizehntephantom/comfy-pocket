# ===== НАСТРОЙКИ РЕНДЕРА =====
# Меняй значение после знака = . Строки с # — комментарии, игнорируются.
# Имена model/lora должны совпадать с именами файлов (без .safetensors можно).

model = anillustriousXL_v14
lora = sasaminanimev6
lora_weight = 0.8

steps = 30
cfg = 5
width = 1024
height = 1536

# сколько картинок за один запуск (генерятся по очереди, папка откроется когда все готовы)
count = 1
# ===== ТОЛЬКО ДЛЯ РЕНДЕРА ПО ПОЗЕ (run_depth.py / РЕНДЕР_ПОЗА.bat) =====
# Насколько жёстко держать позу с картинки-донора.
#   0.4-0.5 — модели больше свободы (мягкая подсказка)
#   0.6     — как на tensor.art (рекомендую)
#   0.8+    — почти калька с донора, может тянуть и его телосложение
depth_strength = 0.6

# Донор берётся сам из папки  depth\donor\  (самая свежая картинка).
# Эта строка нужна, ТОЛЬКО если хочешь жёстко прибить конкретный файл:
# depth_image = D:\Downloads\poza.jpg

# Холст сам подстраивается под пропорции донора (иначе позу растягивает и ломает).
# Поставь depth_fit = config, если хочешь жёстко width/height сверху.
depth_fit = donor

# ===== ТОЛЬКО ДЛЯ РЕНДЕРА ПО КОНТУРУ (run_canny.py / РЕНДЕР_КОНТУР.bat) =====
# Насколько жёстко держать контур с картинки-донора (силуэт, складки, черты).
#   0.4-0.5 — мягкая подсказка;  0.6 — как на tensor.art;  0.8+ — почти калька.
# Если строки нет — берётся depth_strength.
canny_strength = 0.6
# Пороги детектора контуров (реже трогать): ниже low — больше линий/шума,
# выше high — только самые резкие грани. 100/200 — стандарт.
canny_low = 100
canny_high = 200
# Донор берётся сам из папки  canny\donor\  (самая свежая картинка).
# canny_image = D:\Downloads\lineart.jpg   (только для жёсткого файла)
# canny_fit = donor  (холст под пропорции донора; config — жёстко width/height)

# ===== ТОЛЬКО ДЛЯ РЕНДЕРА ПО ЦВЕТУ (run_color.py / РЕНДЕР_ЦВЕТ.bat) =====
# Держит РАСКЛАДКУ ЦВЕТА с донора: где светлое пятно, где тёмный угол, какая
# палитра. Рисунок донора стирается в мозаику — форму и линии это не держит.
# ВНИМАНИЕ: цифры тут НЕ такие, как у позы и контура. Адаптер весит 158 МБ
# против 2.4 ГБ у тех, и на привычных значениях он почти не слышен.
# Померено на одном сиде 2026-08-19:
#   0.5-1.0 — разницы с выключенным почти не видно
#   1.5     — палитра донора приходит, рисунок остаётся чистым  <- рабочее
#   2.0     — цвет берёт своё, но картинка начинает мылиться
#   3.0     — разваливается совсем, в кадре видно клетки мозаики
# Если строки нет — берётся depth_strength.
color_strength = 1.5

# Крупность мозаики. Короткая сторона делится на 64, отсюда число клеток:
#   512  — 8 клеток, как на tensor.art (только общее настроение)
#   1024 — 16 клеток (композиция держится заметно точнее)
color_resolution = 512

# По какому куску имени искать модель в ComfyUI\models\controlnet\ .
#   t2i   — адаптер t2i-adapter_xl_canny, как на tensor.art (нужно докачать)
#   canny — наш большой xinsir, если хочется попробовать без закачки
color_model = t2i

# Донор берётся сам из папки  color\donor\  (самая свежая картинка).
# color_image = D:\Downloads\palette.jpg   (только для жёсткого файла)
# color_fit = donor  (холст под пропорции донора; config — жёстко width/height)